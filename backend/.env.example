PORT=5050
# Optional: Paste your MongoDB connection URI (e.g. Atlas or local)
# If left commented or empty, FlowTask automatically uses local todos.json file storage.
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/flowtask?retryWrites=true&w=majority
MONGODB_URI=
